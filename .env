# Scoracle Data - Environment Template
#
# This file is committed and holds safe defaults / placeholders only.
# Copy to .env.local and fill in real values there (.env.local is gitignored).
#
# Both files are loaded at startup; .env.local wins on conflict.

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
# Priority in Go config: DATABASE_PRIVATE_URL > DATABASE_URL
# Local dev default (KingSpec NVMe Postgres):
#   postgresql://scoracle:YOUR_PASSWORD@localhost:5432/scoracle?sslmode=disable
DATABASE_PRIVATE_URL=
DATABASE_URL=

# Go database pool
DB_POOL_MIN_CONNS=2
DB_POOL_MAX_CONNS=25
DB_POOL_MAX_LIFE_MINUTES=30

# -----------------------------------------------------------------------------
# API server
# -----------------------------------------------------------------------------
ENVIRONMENT=development
API_HOST=0.0.0.0
API_PORT=8000

# CORS
CORS_ALLOW_ORIGINS=http://localhost:3000,http://localhost:4321,http://localhost:5173
CORS_PRODUCTION_ORIGINS=https://scoracle.com,https://www.scoracle.com

# Cache and rate limiting
CACHE_ENABLED=true
CACHE_BACKEND=memory
CACHE_WARMUP_ENABLED=true
RATE_LIMIT_ENABLED=true
RATE_LIMIT_REQUESTS=100
RATE_LIMIT_WINDOW=60

# -----------------------------------------------------------------------------
# Provider keys (fill in .env.local)
# -----------------------------------------------------------------------------
SPORTMONKS_API_TOKEN=
BALLDONTLIE_API_KEY=
API_SPORTS_KEY=

# -----------------------------------------------------------------------------
# Integrations (fill in .env.local)
# -----------------------------------------------------------------------------
# News + transfer heat derive from the Google RSS corpus.
# TODO: add additional-language RSS searches.

FIREBASE_CREDENTIALS_FILE=

# -----------------------------------------------------------------------------
# Mobile auth (device-identity JWT) — set JWT_SECRET in .env.local
# -----------------------------------------------------------------------------
# Generate a strong secret: openssl rand -base64 48
# Empty here => /auth/* endpoints return 503; the rest of the API is unaffected.
JWT_SECRET=
JWT_ACCESS_TTL_MINUTES=30
JWT_REFRESH_TTL_DAYS=90

# -----------------------------------------------------------------------------
# Local inference (Ollama) — the derived-products diviner
# -----------------------------------------------------------------------------
# mistral:7b since the L7 cutover off gemma4:e4b: fits the 8GB GPU (~5.2 GB, ~19
# tok/s), instruction-following + multilingual. `ollama pull mistral:7b` on each
# machine. Override per-host in .env.local; the Go + Rust configs default to this.
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=mistral:7b
