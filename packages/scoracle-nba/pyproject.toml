[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "scoracle-nba"
version = "0.1.0"
description = "NBA data seeder using BallDontLie API"
requires-python = ">=3.11"
dependencies = [
    "asyncpg>=0.29.0",
    "httpx>=0.27.0",
    "click>=8.0.0",
]

[project.scripts]
scoracle-nba = "scoracle_nba.cli:cli"
