# Backend — DB pool 10→25 in the env (Phase A deploy follow-up)

**Date:** 2026-06-19 · Backend (config; prod rebuilt + restarted).

## Goal
Phase A (`195c8f1`) raised the **code** default `DB_POOL_MAX_CONNS` 10→25 for the eager fan-out, but prod
sets the env var explicitly, which overrode the default — the post-deploy boot log showed `max_conns=10`.
Apply 25 in the env so the pool sizing actually takes effect.

## What Was Done
- Deployed Phase A to prod: rebuilt `go/bin/scoracle-api` (backup → `*.bak-preeager`), `systemctl --user
  restart scoracle-api`. Clean boot — DB connected, all 3 LISTEN/NOTIFY listeners up, maintenance tickers
  started, no prepare errors / degraded mode. The no-on-read-Gemma + single-flight code is live.
- `.env.local` (prod, gitignored) + `.env` (committed template): `DB_POOL_MAX_CONNS` 10→25; restarted →
  boot log now `max_conns=25`.
- Confirmed Postgres headroom first: `max_connections=100`, ~15 in use → 25 is safe.

## Files Changed
`.env` (committed). `.env.local` is gitignored (prod-only).

## Verification
`systemctl restart` → health `200`; boot log `Database connected … max_conns=25`, `environment=production`,
no errors. Frontend eager mount-all already deployed (Worker `7558ffd0`); `scoracle.com` → 200.

## Result
The eager-readiness is fully live in prod: A1 (no on-read Gemma synth), A2 (single-flight), A3 (momentum
TTL), **A4 pool=25** (was silently overridden by the explicit env var). Backend + frontend eager push complete.
