# Install the package itself
-e .

httpx>=0.25.0
pydantic>=2.4.2
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Postgres / Neon driver (required for pg_connection.PostgresDB)
psycopg[binary]>=3.1
psycopg-pool>=3.1

# API framework and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
msgspec>=0.18.0
