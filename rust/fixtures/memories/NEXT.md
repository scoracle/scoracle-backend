# Next milestone: reliable comparative Scout readings

Checkpoint: September 14, 2026. The first rejected production request, source trace and a qualified
thin-sample follow-up are archived. Local follow-up now separates audit context from writer
context, admits current performance/roster reports to Scout, suppresses ineligible or unidentified
legacy ranks, records raw provider telemetry, and routes settled source evidence to the existing
identity owner. Deployment and canonical source coverage repair remain outstanding.

The product goal is a supported explanation of how contribution changes across
teams and seasons. A provider-to-scorer transition is one possible finding;
continued strength in both roles, no meaningful change and insufficient evidence
are equally valid. Memory supplies the comparison, and the character interprets it.

## Completed: one complete production request

Morgan Rogers was traced through the real Scout builder from current source rows through
selected memories and fresh evidence to the exact provider request and raw reply. The capture
includes:

- The full package, retained groups, omissions and source references.
- Current and prior statistical windows, recorded minutes and competition.
- The exact system/user messages, model settings and input fingerprint.
- Actual full-request token counts, completion reason and unmodified output.

See `probes/2026-09-14-production/REVIEW.md` and its exact JSON artifacts. The initial request
failed editorial review and exposes the defects. The qualified follow-up includes current reports,
changes the material key, fits the surface and makes no directional claim from one appearance.

## Repair missing evidence through its existing owner

Rogers has a stale Villa identity projection alongside Chelsea statistical and
reporting evidence. His captured current statistical row has one appearance and
no goals value. Resolve these as source/ingestion problems before drawing a
seasonal role-change conclusion.

Trace the upstream fixture/box-score coverage, then verify the current aggregate
against dated sources. Keep league and cup counts separate. Metadata confirmation
needs a route to the existing identity reconciliation owner even after rumor heat
falls; a correct prediction is not itself authoritative confirmation. Preserve
old affiliations and effective-date precision when reconciling the current club.

Do not hand-edit the rendered memory or substitute a guessed zero. The shared
adapter should read corrected source records and produce a changed material hash.
The local FPL owner now creates and binds missing official source fixtures and reports unresolved
ones in its funnel. Operational rollout and verification of the repaired aggregate remain separate
from this local checkpoint.

## Make the comparison readable before tuning the voice again

Inspect which administrative timestamps, calendar fields and source envelopes
still reach the writer. Keep a complete audit package, while presenting only the
dates and facts needed for the sporting interpretation. Observation dates must
remain distinguishable from signing dates, and reporting periods from fixtures.
Keep the named historical team, matching measurement identities, denominators,
uncertainty and material conflicts together.

Use the existing Scout character, form and route as the control. Change one input
presentation at a time. The rejected s34 experiment showed that a voice-only edit
did not resolve temporal errors, unsupported causes or recitation of metadata.
Avoid adding outcome-specific wording, keyword targets or speculative role labels.

## Evidence needed to proceed

The Morgan Rogers thin-sample case now passes. Review the remaining small set covering a supported
change in contribution, a stable profile and a disputed affiliation. Include current news in each
actual production request. Require:

1. Every comparison uses compatible measures, competition and time bases.
2. No invented dates, missing-as-zero values, or inference of actual playing time
   from source coverage. Unsupported role changes remain unresolved.
3. The reading explains sporting significance rather than reciting the package.
4. Complete output fits the surface and the full provider context window.
5. Corrected source facts change the generation key; repeated observations or
   a new prior card alone do not produce a refresh loop.

Review individual claims against retained sources, not only parser acceptance.
Keep raw failures. Once the Scout comparison is reliable, repeat this complete
request check for the Journalist and Insider before broadening the other voices.

## Checkpoint validation

- 466 tests passed with `cargo test --all-targets --quiet`.
- `cargo clippy --all-targets -- -D warnings` passed.
- `cargo fmt --all -- --check` and `git diff --check` passed.
- Source adapter queries passed read-only, repeatable-read database checks.
- Twelve exploratory model calls are archived across three reviews. None passed
  editorial review; the candidate voice was rejected and the runtime retained s33.
- The final production-built thin-sample request passed claim review and the card-surface parser;
  its exact provider request and unmodified response are archived.
- No deployment, migration, metadata repair, queue regeneration or corpus rewrite
  was performed.

See [implementation notes](README.md), [source adapter checks](source-adapter-checks-2026-09-14.json),
[compact context review](probes/2026-09-14-v2/REVIEW.md), and
[voice comparison](probes/2026-09-14-voice/REVIEW.md).
