[build]
builder = "DOCKERFILE"
watchPatterns = [
    "scoracle_seed/**",
    "services/**",
    "shared/**",
    "pyproject.toml",
    "Dockerfile",
    "railway.toml",
]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
